# 跨商品组合套餐促销（"M 件 N 元"）设计方案 v3

> 本文件是已批准的实施计划。v3 经对抗性审查 + 低 QPS 单体约束修订：**合并 tier / 复用 operation_log / scope 纯 SKU 级 / goods_amount 实付语义 / final_amount 生成列**，砍掉一切非必要预留。

## Context（为什么做）

宠物店需要做促销：把多种**同价**商品（如多款 12 元商品，无论单 SKU 还是多 SKU）放进一个"活动池"，顾客购物车里任意凑够 M 件即享套餐价 N 元（3 包 30 元）。要求**有扩展性、不容易出问题**。

当前系统**零促销代码**：算价是纯"逐 SKU 单价 × 数量"（`PriceCalculationServiceImpl.calculateItems`），`order_item` 只有"单价×数量"语义。本方案在不动摇现有会员价语义的前提下，新增独立促销层。

**部署约束**：QPS 低（用户 ≤200，同时在线 ≤50），单体系统。因此**不做缓存、不做分布式锁、不为不确定的未来提前建表/建字段**。一切复杂度必须有当下理由。

### 已拍板的语义决策
1. **跨商品同价池**：一个促销 = 一组**同价 SKU** + 1 个档位（M 件 N 元），购物车内命中件数汇总凑组。
2. **整数组套餐**：凑满 M 件一组 = N 元，**可重复**，余量按原价。买 3=30、买 6=60、买 4=42、买 2=24（不够组全原价）。
3. **单档**：每个 promotion 有且仅有 1 个档位（`min_quantity`/`bundle_price` 直接存在 promotion 表，**不建独立 tier 表**）。
4. **同价池**：池内所有 SKU 的 `price` 必须相等（validate 阻断）→ 分摊按**件均摊**，"哪几件进套餐"无差异。
5. **会员价空**：参与套餐的 SKU 其 `member_price` 必须为空 → 命中促销件走现有 GOODS 分支天然得原价，**算价链路无需互斥判断、无需 member_overlap 字段**。

### 首版范围约束
- **只允许 GOODS 参与**（validate 强制）。SERVICE 涉及预约/时长，凑组语义复杂，先排除。
- **不支持部分退款**：首版仅整单退（全额）。分摊按整组，退一件破坏凑组。退款/明细展示照搬订单快照，不重算。
- **SKU 唯一活动**：同一 SKU 同时只能在一个 ENABLED 促销池（validate 阻断第二个），消除算价命中歧义。
- **删除即停用**：不物理删除促销，"删除" = 置 DISABLED，历史 `order_item.promotion_id` 永不悬空、审计完整。
- **不做限购**：无限购/限量字段与逻辑（YAGNI，需要时再加）。
- **起送价按原价**：促销不影响起送资格（`DeliveryService` 用 originalPrice，不改）。

---

## 一、数据库设计

新增 `backend/sql/migration_promotion_v1.sql`。

### 1.1 新增表（仅 2 张）

```sql
-- 促销活动（M件N元，单档；字段直接含档位，无独立 tier 表；无 type/priority 预留）
CREATE TABLE IF NOT EXISTS promotion (
    id            BIGINT AUTO_INCREMENT PRIMARY KEY,
    name          VARCHAR(128)  NOT NULL COMMENT '活动名，如「3包30元」',
    min_quantity  INT           NOT NULL COMMENT 'M：凑够多少件成一组',
    bundle_price  DECIMAL(10,2) NOT NULL COMMENT 'N：本组总价',
    status        VARCHAR(16)   NOT NULL DEFAULT 'ENABLED' COMMENT 'ENABLED / DISABLED',
    start_time    DATETIME      NULL COMMENT '生效起始，NULL=立即',
    end_time      DATETIME      NULL COMMENT '生效截止，NULL=长期',
    sort          INT           NOT NULL DEFAULT 0,
    remark        VARCHAR(255)  NULL,
    create_time   DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time   DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_promo_status_time (status, start_time, end_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='促销活动 M件N元';

-- 活动池：一组同价 SKU（纯 SKU 级，无 product_id 冗余、无 NULL 语义）
CREATE TABLE IF NOT EXISTS promotion_scope (
    id            BIGINT AUTO_INCREMENT PRIMARY KEY,
    promotion_id  BIGINT NOT NULL,
    sku_id        BIGINT NOT NULL COMMENT '入池SKU（须属 GOODS 在售商品）；池内所有SKU的price必须相等、member_price必须为空',
    create_time   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_promo_sku (promotion_id, sku_id),
    INDEX idx_scope_sku (sku_id),
    INDEX idx_scope_promo (promotion_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='促销活动池（同价SKU集合）';
```

> **不建 `promotion_tier`**（单档合并进 promotion）、**不建 `promotion_log`**（复用现有 `operation_log`，见 2.5）。

### 1.2 扩展现有表

```sql
-- order_item：final_amount 用 GENERATED 列，自动 = subtotal − discount_amount，永不不一致、历史自动回填
ALTER TABLE order_item
    ADD COLUMN promotion_id            BIGINT       NULL COMMENT '命中促销 promotion.id 快照（NULL=未参与）' AFTER sku_id,
    ADD COLUMN promotion_name_snapshot VARCHAR(128) NULL COMMENT '促销名快照，防改名' AFTER promotion_id,
    ADD COLUMN discount_amount         DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '本行分摊优惠' AFTER subtotal,
    ADD COLUMN final_amount            DECIMAL(10,2) GENERATED ALWAYS AS (subtotal - discount_amount) STORED COMMENT '本行实付 = subtotal − discount_amount' AFTER discount_amount;

-- orders：promotion_discount 仅展示/统计用（total 已含此优惠，见等式）。goods_amount 语义保持"用品实付"
ALTER TABLE orders
    ADD COLUMN promotion_discount DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '促销优惠总额（展示/统计用）' AFTER service_amount;
```

> ⚠️ **实现必改**：`final_amount` 是 GENERATED 列，`OrderItemMapper.xml` 的 `insertBatch` 列清单**必须排除 `final_amount`**，否则 MySQL 拒绝插入。历史 `order_item`：`discount_amount=0` → `final_amount` 自动 = `subtotal`，无需手工回填。

### 1.3 金额等式（对账基准）

- `order_item`：`deal_price`（命中促销件=原价）、`subtotal = deal_price × quantity`、`discount_amount` = 分摊优惠、`final_amount = subtotal − discount_amount`（GENERATED）。
- `orders`：**`goods_amount = Σ final_amount(GOODS)`（实付！）**、`service_amount = Σ subtotal(SERVICE)`（首版 SERVICE 不参与促销）、`promotion_discount = Σ discount_amount`。
- **`total_amount = goods_amount + service_amount + delivery_fee`（公式与现状完全一致，不改）**。
- 一致性：`goods_amount` 语义维持"用品实付"不变 → 所有现有读 `goods_amount` 的代码（Admin 详情/企微通知/H5 订单页/报表）**零改动**。

同步更新 `CLAUDE.md` 表结构字典（orders/order_item 加字段，追加 promotion/promotion_scope 两表）。

---

## 二、后端实现

### 2.1 算价核心改造 — `PriceCalculationServiceImpl.calculateItems`

**dealPrice 分支完全不改**（命中促销件因 memberPrice 空天然得 price）。**goodsAmount 累加推迟到分摊之后**（按实付累加）。

```
calculateItems(items, userPhone):
  discountRate = resolveDiscountRate(userPhone)            // 不变
  actives = promotionService.getActivePromotionsForCalc()  // 实时查库不缓存；返回 promotion(min_quantity/bundle_price) + scope(sku_id集合)；按 sort,id 升序

  // ① 算 dealPrice（原逻辑）+ 标记命中
  for each input:
     解析 product/sku（不变）
     hitPromo = 第一个 scope 含 input.skuId 的 active promotion   // 纯 sku 匹配，按 sort,id 序
     dealPrice = 原 GOODS/SERVICE 会员分支                         // 命中促销件 = price
     subtotal = dealPrice × quantity
     暂记 ci.promotionId / promotionName（命中时）
     // 此处不累加 goodsAmount

  // ② 单档凑组 + 均摊分摊（填各 ci.discountAmount）
  promotionDiscount = applySingleTierBundle(calculatedItems, actives)

  // ③ 按实付累加
  goodsAmount   = Σ (subtotal − discountAmount) for GOODS items
  serviceAmount = Σ subtotal for SERVICE items
  resultMap 加 goodsAmount, serviceAmount, promotionDiscount
```

**`applySingleTierBundle`（单档 + 同价均摊，确定可复现）**：

```
按 promotionId 分组命中 item 索引（保留购物车顺序）
totalDiscount = 0
for each (promoId, idxs):
    p = actives[promoId]
    P = 命中件统一原价（同价池，取任一命中 sku.price；配置保证相等）
    totalQty = Σ calculatedItems[i].quantity
    groups = totalQty // p.min_quantity            // 整除，可重复
    if groups == 0: continue                        // 凑不齐，无优惠
    bundleQty   = groups × p.min_quantity
    bundlePrice = groups × p.bundle_price
    discount = bundleQty × P − bundlePrice
    if discount <= 0: continue                      // 防负兜底
    totalDiscount += discount

    need = bundleQty; allocated = 0; lastAssigned = -1
    for i in idxs（顺序）:
        if need <= 0: break
        take = min(calculatedItems[i].quantity, need)
        share = discount × take / bundleQty         // HALF_UP 2位
        if share > 该行 subtotal: share = subtotal    // 防单行超额（运行时不同价兜底）
        calculatedItems[i].discountAmount = share
        allocated += share; need -= take; lastAssigned = i
    if lastAssigned >= 0:
        calculatedItems[lastAssigned].discountAmount += (discount − allocated)  // 舍入兜底
return totalDiscount
```

要点：同价 → `bundleQty×P` 确定 → discount 与选取无关 → 均摊成立；余量件 `discountAmount=0`；尾差补到最后一个分到优惠的 item，保证 `Σ discountAmount == promotion_discount`。

### 2.2 DTO 扩展 — `CalculatedItemResult`

新增：`Long promotionId`、`String promotionName`、`String discountAmount`（默认"0.00"）、`String finalAmount`（默认=subtotal）。

### 2.3 下单落库改造 — `OrderServiceImpl.createOrder`

- `calculateItems` 返回 `promotionDiscount` → 写入 `orders.promotion_discount`。
- **`totalAmount = goodsAmount + serviceAmount + deliveryFee`（L163 原表达式不变，因为 goodsAmount 已是实付）**。
- 落 `order_item`：带 `promotionId`、`promotionName`（快照）、`discountAmount`；**不写 `final_amount`**（GENERATED 自动算）。
- 返回 Map 加 `promotionDiscount`（展示用）。

预览入口 `AppCartController.calculate` 共用 service，自动受益、无需改动。

### 2.4 展示侧 — `AppProductController`

只加标签不重算：新增 `GET /api/app/promotions/active`（返回 `name` + `skuId[]`/`productId[]` + 文案"M件N元"），H5 拉取缓存、按 id 匹配显示角标。商品列表/详情接口不 join 促销。

### 2.5 新增后端类清单（瘦身版）

| 层 | 内容 |
|---|---|
| entity | `Promotion`（含 `minQuantity`/`bundlePrice`）、`PromotionScope`。**无 Tier、无 Log 实体**。 |
| mapper | `PromotionMapper`、`PromotionScopeMapper` + XML。变更审计**复用现有 `OperationLogMapper`**（写 `action=CREATE_PROMOTION/UPDATE_PROMOTION/UPDATE_PROMOTION_STATUS/DELETE_PROMOTION`，`target=促销名`，`before_val/after_val` 存 JSON 文本）。 |
| service | `PromotionService` + Impl：`list / getDetail / create / update / updateStatus / delete`；`getActivePromotionsForCalc()`（实时查库）；`validate()`；每次写变更同步写一条 `operation_log`。 |
| controller | `AdminPromotionController`（`/api/admin/promotions`，BOSS）：`list/get/create/update` + `PUT /:id/status`（**"删除"即置 DISABLED，不物理删**）+ `GET /logs`（查 `operation_log` 中 action 属 PROMOTION\* 的记录，全局列表，不按单活动精确关联——`operation_log` 无 promotion_id 外键，小店够用）；`AppPromotionController`（`/api/app/promotions/active`，免登）。 |

### 2.6 validate 校验规则（失败抛 `BusinessException`）

- `min_quantity ≥ 1`；`bundle_price ≥ 0`。
- **同价池**：scope 内所有 `sku.price` 完全相等。
- **会员价空**：scope 内所有 `sku.member_price` 为 NULL。
- **商品合法**：所选 SKU 属于 `type=GOODS` 且 `status=ON_SALE` 的商品；至少 1 个 SKU。
- **SKU 唯一活动**：同一 SKU 同时只能被一个 ENABLED 促销关联；新增/启用时若该 SKU 已在另一个 ENABLED 促销池，阻断（停用 promotion 不占名额）。
- 防亏本提示：`bundle_price / min_quantity`（单件均价）> 池内原价时告警（不阻断）。

> 相比 v2 删除的校验：单档 tier 校验（已合并）、scope 全/指定 SKU 混用与去重（sku_id NOT NULL + UNIQUE 自然保证）。

---

## 三、Admin 前端（`frontend/admin`）

`PromotionPage.vue` = `ProductPage` 列表+dialog 骨架 + `SystemConfigPage` 脏数据离开提示。

- **列表**：名称、档位（"3件/¥30"）、状态 switch、有效期、池内 SKU 数、操作（编辑/停用，停用即"删除"）。
- **dialog**：名称；档位两字段（`min_quantity` + `bundle_price`）；有效期（`el-date-picker` 可空）；备注；**活动池 = SKU 多选**（候选列出所有 GOODS 在售 SKU，显示"商品名/规格/单价"，便于挑同价 SKU；选中后前端可即时校验同价并提示）；`onBeforeRouteLeave` 离开提示。
- **审计日志**：只读 `el-table`（时间/操作人/action/summary），数据来自 `GET /logs`（全局促销操作日志）。
- **路由** `router/index.ts` 加 `{ path:'promotions', name:'Promotion', component:()=>import('@/views/PromotionPage.vue'), meta:{ title:'促销管理', icon:'Present', roles:['BOSS'] } }`。
- **`api/promotion.ts`**：`getPromotions / getPromotion / createPromotion / updatePromotion / updatePromotionStatus / getPromotionLogs`（"删除"调 `updatePromotionStatus` 置 DISABLED，无独立 delete）。
- **`types/index.ts`**：`Promotion / PromotionScope`（无 Tier/Log）。

---

## 四、H5 前端（`frontend/h5`，最小改动）

- **购物车抽屉防坑（必做）**：`CartDrawer.vue`/`CartBar.vue` 现用 `cartStore.totalAmount`（纯前端累加），促销后不准 → 改为打开/变化时调 `/api/app/cart/calculate` 取真价（前端加 debounce）。合计用返回的 `totalAmount`（已是实付）。
- **`PriceBreakdown.vue`**：加"促销优惠 −¥X"行（`CartCalculateResult` 加 `promotionDiscount`）。
- **促销标签**：`ProductCard.vue`/`SkuSelectorPopup.vue` 用 `/api/app/promotions/active` 按 id 匹配显示"M件N元"角标。
- **`types/index.ts`**：补 `promotionDiscount`、`PromotionSummary`。

---

## 五、验证（端到端）

**算价单测**（核心）：
- 同价池 12 元×5、档位[3件30]：1 组+余 2 → `discount=6`；进套餐 3 件各分 2.00、余 2 件 0；`goods_amount=Σfinal=54`、`promotion_discount=6`、`total` 正确。
- 买 3 → 优惠 6；买 6 → 优惠 12（两组）；买 2 → 优惠 0。
- 买 4：3 件套餐 30 + 1 件原价 12 = `total=42`、`goods_amount=42`、`promotion_discount=6`。
- 舍入兜底：`discount/bundleQty` 除不尽时断言 `Σ discountAmount == promotion_discount` 且无 item 负值。
- `goods_amount == Σ final_amount(GOODS)` 恒等。

**接口手测**：`/api/app/cart/calculate` 凑够/不够组；`/api/app/orders` 下单后查 `order_item`（含 generated `final_amount`）、`orders.promotion_discount`、`total`；Admin 建活动（故意配不同价/带会员价 SKU，确认 validate 阻断）、启停、查 `operation_log`。

**前端**：购物车凑组抽屉合计 = 结算页；`PriceBreakdown` 优惠行；下单成功页金额一致；Admin CRUD/SKU 多选/同价提示/审计日志。

---

## 六、风险与边界

1. **算价一致**：预览与下单共用 `calculateItems` → 天然一致；展示侧只加标签不重算。
2. **goods_amount 实付语义**：`total` 公式不改、`goods_amount` 仍是实付 → 现有所有读金额的代码零改动（这是 v3 关键改进，规避静默错乱）。
3. **final_amount GENERATED**：自动 = `subtotal − discount_amount`，永不不一致、历史自动回填；代价是 `insertBatch` 须排除该列。
4. **不缓存**：`getActivePromotionsForCalc` 每次实时查库；活动数小、QPS 低，可接受。
5. **不部分退款**：分摊按整组；首版仅整单退，展示照搬快照不重算。
6. **⚠️ 已知限制（首版接受，"仅靠配置校验"决策）**：同价池与会员价空**仅配促销时 validate**。事后编辑商品改 SKU `price` 或补 `member_price` 会破坏假设导致算价基准错乱。**本版不做运行时防御**，依赖运营规范。可选增强：商品编辑页对命中活动池的 SKU 加软提示。
7. **operation_log 无 promotion_id**：促销审计只能全局列表，无法精确按单活动关联（小店够用）。
8. **历史订单**：新字段默认值/GENERATED 保证老数据零影响。
9. **起送价按原价**：促销不影响起送资格（`DeliveryService` 用 originalPrice，不改）——商家让利不让凑起送变难。
10. **SKU 唯一活动 + 仅停用**：validate 阻断同一 SKU 入多个 ENABLED 促销；删除即停用，历史 `order_item.promotion_id` 永不悬空。

## 七、实施顺序
1. DB migration（2 新表 + 2 表 ALTER）+ `Promotion`/`PromotionScope` 实体与 Mapper；改 `OrderItemMapper.insertBatch` 排除 `final_amount`。
2. `PromotionService` + `getActivePromotionsForCalc`（实时查库）+ 算价改造（单档均摊、实付累加）+ 单测。
3. `createOrder` 落库（promotion_discount / order_item 新字段）+ `AppCartController` 联调。
4. `AdminPromotionController`（CRUD + status + operation_log 审计）+ Admin 页面。
5. H5 防坑 + 标签 + 明细行。
6. 端到端验证 → 更新 `CLAUDE.md` 字典 → `/my-commit`。
