version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: petorder-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root123}
      MYSQL_DATABASE: petshop_order
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
      TZ: Asia/Shanghai
    volumes:
      # 用 01/02 数字前缀确保 schema 先于测试种子执行（MySQL 按目录内文件名排序依次执行）
      - ./sql/init.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./sql/init-seed-dev.sql:/docker-entrypoint-initdb.d/02-seed-dev.sql
      - mysql_data:/var/lib/mysql
    command: >
      --default-authentication-plugin=caching_sha2_password
      --character-set-client-handshake=FALSE
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --init-connect='SET NAMES utf8mb4'
      --default-time-zone=+08:00

volumes:
  mysql_data:
