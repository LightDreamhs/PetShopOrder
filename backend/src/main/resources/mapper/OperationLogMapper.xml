<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petshop.order.mapper.OperationLogMapper">

    <select id="selectPageList" resultType="com.petshop.order.entity.OperationLog">
        SELECT ol.*, au.username
        FROM operation_log ol
        LEFT JOIN admin_user au ON au.id = ol.user_id
        <where>
            <if test="userId != null">
                AND ol.user_id = #{userId}
            </if>
            <if test="action != null and action != ''">
                AND ol.action = #{action}
            </if>
            <if test="startTime != null and startTime != ''">
                AND ol.create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND ol.create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY ol.create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (user_id, action, target, before_val, after_val)
        VALUES (#{userId}, #{action}, #{target}, #{beforeVal}, #{afterVal})
    </insert>

</mapper>
