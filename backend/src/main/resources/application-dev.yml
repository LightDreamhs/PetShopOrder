# 本地开发 profile（SPRING_PROFILES_ACTIVE=dev，默认）
# 放行前端本地开发服务器与内网调试地址的跨域请求
app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:3001
      - http://192.168.*.*:3000
      - http://192.168.*.*:3001
