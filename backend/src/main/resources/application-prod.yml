# 生产 profile（部署时通过环境变量 SPRING_PROFILES_ACTIVE=prod 激活）
#
# 生产前后端同源（nginx 反代 /api/），默认不触发 CORS；如需对接其它前端域名，
# 向 app.cors.allowed-origins 追加，或通过环境变量覆盖。
#
# 所有敏感配置（DB 密码、SMS AK/SK、admin 初始口令、AES key 等）通过 .env.prod
# 环境变量注入，见 deploy/.env.prod.example。
app:
  cors:
    allowed-origins: []
