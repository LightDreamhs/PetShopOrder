# VCS / IDE
.git
.idea
.vscode
.obsidian
.claude

# 后端 / 部署 / 文档 / 规划（前端镜像不需要，缩小上下文）
backend/
deploy/
docs/
backend-planning/

# 依赖与本地构建产物（前端 dist 例外：本地构建后上传，镜像直接 COPY，见 deploy/Dockerfile.frontend）
**/node_modules
**/dist
!frontend/h5/dist
!frontend/admin/dist

# 文档
*.md

# 根目录敏感文件（绝不进镜像）；frontend/.env.production 不在此列，前端构建需要
/.env
/.env.prod
短信验证服务Access key.txt
飞书订单通知群webhook.txt
